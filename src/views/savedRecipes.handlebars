{{> privateNav}}

<div class="flex justify-center mb-4">
    <div class="flex justify-center py-4 mx-3">
        <h1 class="pacifico justify-center text-2xl md:text-3xl lg:text-4xl text-center mealjoy-green">
            Your saved recipes
        </h1>
    </div>
</div>

{{#if recipes}}
<div class="grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-2 p-2">
    {{#each recipes}}
    {{> savedRecipe}}
    {{/each}}
</div>
{{else}}
<p>here</p>
{{/if}}